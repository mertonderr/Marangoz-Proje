function otomatikGecis(sayfaAdi, sure) {
    setTimeout(function() {
        window.location.href = sayfaAdi;
    }, sure);
}

document.getElementById("mutfakLink").addEventListener("click", function(event) {
    event.preventDefault(); 
    otomatikGecis("mutfak.html", 1000); 
});

function otomatikGecis(sayfaAdi, sure) {
    setTimeout(function() {
        window.location.href = sayfaAdi;
    }, sure);
}

document.getElementById("banyoLink").addEventListener("click", function(event) {
    event.preventDefault(); 
    otomatikGecis("banyo.html", 1000); 
});

function otomatikGecis(sayfaAdi, sure) {
    setTimeout(function() {
        window.location.href = sayfaAdi;
    }, sure);
}

document.getElementById("portmantoLink").addEventListener("click", function(event) {
    event.preventDefault(); 
    otomatikGecis("portmanto.html", 1000); 
});

function otomatikGecis(sayfaAdi, sure) {
    setTimeout(function() {
        window.location.href = sayfaAdi;
    }, sure);
}

document.getElementById("gardropLink").addEventListener("click", function(event) {
    event.preventDefault(); 
    otomatikGecis("gardrop.html", 1000); 
});

function otomatikGecis(sayfaAdi, sure) {
    setTimeout(function() {
        window.location.href = sayfaAdi;
    }, sure);
}

document.getElementById("kapıLink").addEventListener("click", function(event) {
    event.preventDefault(); 
    otomatikGecis("kapı.html", 1000); 
});